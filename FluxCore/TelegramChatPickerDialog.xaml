<Window x:Class="FluxCore.TelegramChatPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Telegram — Choose Chats to Monitor"
        Height="440" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        Background="#1A1A21"
        Foreground="White"
        FontFamily="Consolas">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select chats to monitor. Leave all unchecked to monitor every DM and group."
                   Foreground="#AAB0C0" FontSize="10" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="✔ All" Click="SelectAll_Click"
                    Background="#20303F" Foreground="#80C0A0"
                    BorderBrush="#304050" BorderThickness="1"
                    Padding="10,3" Margin="0,0,6,0" Cursor="Hand" FontSize="10"/>
            <Button Content="✕ None" Click="DeselectAll_Click"
                    Background="#20303F" Foreground="#80A0B0"
                    BorderBrush="#304050" BorderThickness="1"
                    Padding="10,3" Cursor="Hand" FontSize="10"/>
        </StackPanel>

        <ListBox x:Name="ChatList" Grid.Row="4"
                 Background="#0D0D14"
                 BorderBrush="#304050"
                 BorderThickness="1"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 Padding="4"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click"
                    Background="#20303F" Foreground="#80A0B0"
                    BorderBrush="#304050" BorderThickness="1"
                    Padding="12,4" Margin="0,0,8,0" Cursor="Hand"/>
            <Button Content="OK" Click="Ok_Click"
                    Background="#00887A" Foreground="White"
                    BorderThickness="0"
                    Padding="16,4" Cursor="Hand"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
