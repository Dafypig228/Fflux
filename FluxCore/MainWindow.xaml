<Window x:Class="FluxCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FluxCore Jarvis" 
        Height="400" Width="350"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        Topmost="True"
        ShowInTaskbar="False"  MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border x:Name="MainBorder" 
            Background="#CC101010" 
            BorderBrush="#FF00FFD1" 
            BorderThickness="1" 
            CornerRadius="10" 
            Padding="10">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="FluxCore" Foreground="#FF00FFD1" FontWeight="Bold" FontSize="14"/>
                <Ellipse Width="8" Height="8" Margin="8,0,0,0" Fill="#FF00FFD1">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#FF00FFD1" BlurRadius="10" ShadowDepth="0"/>
                    </Ellipse.Effect>
                </Ellipse>
            </StackPanel>

            <ListView x:Name="ChatList" Grid.Row="1" 
                      Background="Transparent" 
                      BorderThickness="0" 
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#33FFFFFF" CornerRadius="5" Margin="0,2" Padding="8">
                            <TextBlock Text="{Binding Text}" 
                                       TextWrapping="Wrap" 
                                       Foreground="White" 
                                       FontSize="12"
                                       FontFamily="Consolas, Segoe UI"/>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <WrapPanel x:Name="ButtonContainer" Grid.Row="2" Margin="0,10,0,0" HorizontalAlignment="Center"/>
        </Grid>
    </Border>

    <Window.Resources>
        <Style x:Key="ModernBtn" TargetType="Button">
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#55FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
</Window>